<?xml version="1.0" encoding="UTF-8"?>
<kmodule xmlns="http://www.drools.org/xsd/kmodule">

    <!-- Baza znanja za Forward Chaining i CEP. -->
    <kbase name="rulesKBase" eventProcessingMode="stream" packages="com.ftn.sbnz.rules.forward, com.ftn.sbnz.rules.cep">
        <ksession name="rulesKSession" default="true"/>
        <ksession name="cepKieSession"/>
    </kbase>

    <!-- BAZA ZNANJA za Rekurzivni Backward Chaining. -->
    <kbase name="backwardKBase" packages="com.ftn.sbnz.rules.backward">
        <ksession name="backwardKSession"/>
    </kbase>

</kmodule>